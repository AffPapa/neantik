# NeAntik 0.6.7 (49)

Локальная проверка профиля стала понятнее: NeAntik показывает privacy-safe
сводку по localStorage, sessionStorage, IndexedDB, service workers и cache,
не читая URL, ключи, значения, cookies, прокси или имена профилей.

Chromium 152 не пересобирался.
