#!/bin/zsh

set -euo pipefail

echo "Исторический wrapper NeAntik 0.3.12 отключён."
echo "Он ссылался на старый публичный DMG и не является проверкой текущего кандидата."
echo "Используйте version-neutral scripts/Run-NeAntik-DMG-Hosted-Verification.command."
echo "Старый релиз остаётся immutable historical evidence и не перезаписывается."
exit 64
