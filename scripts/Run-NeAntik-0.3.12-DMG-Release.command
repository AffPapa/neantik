#!/bin/zsh

set -euo pipefail

echo "Исторический wrapper NeAntik 0.3.12 отключён."
echo "Он мог запускать текущий release-flow с устаревшей версией в тексте."
echo "Используйте version-neutral Release-NeAntik.command после прохождения всех gates."
echo "Никакие артефакты, подписи или публикации не создавались."
exit 64
